# Interface Layer

Reserved for human-facing Jah surfaces — TUIs, web apps, editor extensions.

Not implemented yet. When added, interface code should call `system/engines/cli.py` (or import engine modules directly), not duplicate protocol logic.

Agent guidance remains in `system/agent/`.
